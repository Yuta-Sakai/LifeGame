<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LifeGame.MyPage">
    <StackLayout Padding="0,20,0,0">
        <StackLayout.Children>
            <Grid x:Name="CellGrid" VerticalOptions="Center" HorizontalOptions="Center"  RowSpacing="1" ColumnSpacing="1"/>
                
            <Grid x:Name="ControlGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" BackgroundColor="Lime" TextColor="Black" Text="Start" Clicked="OnStartClicked" />
                <Button Grid.Column="1" BackgroundColor="Lime" TextColor="Black" Text="Stop" Clicked="OnStopClicked" />
            </Grid>
        </StackLayout.Children>
    </StackLayout>
</ContentPage>
